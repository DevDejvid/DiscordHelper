// list of channel IDs with message analyzer enabled (if kept empty, activate for all channels where bot is present)
export const channelIDs: string[] = [];
// ignore messages from users with one of the listed roles (also may be kept empty)
export const roleIDs: string[] = ["123456789", "987654321"];
